# stock-charts 작업 규칙

## 보고서 생성 (reports/, reports_b/) — 위반 시 즉시 중단

### 1. 사용 도구는 단 하나
`reports_b/*.html` 또는 `reports/*.html` 신규/수정 시 **반드시 `.claude/agents/company-analyst.md` 에이전트**를 통해야 한다.
- 임의로 HTML을 직접 작성·요약·축약 금지
- "포맷이 비슷한 다른 템플릿"을 만들어 쓰는 것 금지
- 시간 절약을 위해 섹션을 줄이는 것 절대 금지

### 2. 풀 템플릿 10개 섹션 — 누락 금지
모든 보고서는 아래 섹션을 전부 포함해야 한다 (한국/미국 기준):
1. `1. 주가 흐름` (iframe + 네이버 차트 링크)
2. `2. 기업 개요` (주요 사업 / 기업 기본 정보 / 최근 시장 관심 이유)
3. `3. 주요 연혁` (테이블, 15~20개 항목)
4. `4.사업개요` (핵심 사업 / 기술 3개 + 차별점 + 난도 / 신규 사업)
5. `5. 주요 제품 매출 구성` (테이블, 3개 연도)
6. `6-1 연간 재무` (iframe) + `6-2 분기 재무` (iframe, 한국/미국 한정)
7. `7. 주주 정보` (주주 테이블 + 임원 테이블)
8. `8. 관련 정보` (이미지, `images/available.txt`에 있을 때만)
9. `9-1`~`9-5 관련 기사` (정확히 5건, 각각 다른 주제, 모두 유효 URL)
10. `10. 사업 검토` (경쟁사 비교 테이블 + 분야별 성장성 + 기술장벽)

섹션 번호·제목·순서를 임의로 변경 금지. `<h4>` 헤더 문자열까지 정확히 일치해야 한다.

### 3. 생성 후 self-verify → 통과해야만 푸시 (에이전트가 직접)
HTML을 쓴 **직후 같은 에이전트가** 아래를 반드시 실행하고, 통과해야만 커밋/푸시로 진행한다.
```
python3 scripts/validate_report.py reports_b/<파일명>.html
```
검사 항목:
- 풀 템플릿 10개 섹션 누락 여부
- 본문 끝/중간 툴호출 XML 누출 (`</content>`, `</invoke>`, `</function_calls>`, `<parameter>`, `antml:` 등 — 2026-06-20 사고)

검증 실패 시:
- 절대 커밋·푸시 금지
- 에이전트가 같은 회차에서 원인 수정 후 재검증
- "거의 다 됐다" 같은 판단으로 통과 처리 금지
- "사용자가 검토할 거니까 일단 푸시" 같은 우회 금지 — 푸시는 self-verify 통과 후에만

### 3-1. pre-commit 훅 (이미 설치되어 있음)
`reports_b/*.html` 또는 `reports/*.html` 커밋 시 `scripts/validate_report.py`가 자동 실행되어 깨진 보고서를 차단한다. 신규 클론 환경에서는 한 번만:
```
bash scripts/setup_hooks.sh
```
훅을 우회하는 `--no-verify` 옵션 사용 금지 (사용자가 명시적으로 지시한 경우만 허용).

### 4. README 갱신
보고서 추가 후 `reports_b/README.md`를 손으로 수정하지 말고 자동 갱신 스크립트를 쓴다:
```
python3 scripts/regenerate_readme.py
```

## 과거 사고 기록 (반복 금지)
- **2026-06-19 ~ 06-20**: 우리엔터프라이즈(290) 등 11개가 풀 템플릿 없이 9개 미만 섹션의 축약 형식으로 생성·커밋됨.
  더 거슬러 올라가면 같은 종류 사고가 25개 추가로 있어 총 36개가 깨졌음 (`scripts/validate_report.py --all` 로 식별 가능).
  → 원인: `.claude/agents/company-analyst.md` 를 거치지 않고 인라인으로 HTML을 만들어 버린 것.
  → 재발 방지: 이 파일의 규칙 1~3 + 검증 스크립트로 게이트.

- **2026-06-20**: 우림피티에스(293), 우신시스템(295) 본문 끝에 에이전트 툴호출 XML 단편(`</content>`, `</invoke>`)이 그대로 새어 들어간 채로 커밋됨.
  validator는 섹션 누락만 봤고 XML 누출은 검사하지 않았음 → 본문 정상 + 끝부분 garbage로 통과 처리됨.
  → 재발 방지: `validate_report.py`에 `TOOLCALL_LEAK_PATTERNS` 추가 (2026-06-20). 에이전트는 HTML 작성 직후 본인이 직접 validator 돌려 통과해야 푸시.
